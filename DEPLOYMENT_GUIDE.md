# 🚀 Deployment Guide - CO-PO Lab Attainment Automation

## Quick Start Deployment to Render.com

Your project is now ready for deployment! Follow these simple steps to go live in 5 minutes.

---

## **Step 1: Create Render Account**

1. Go to [render.com](https://render.com)
2. Click **"Sign Up"** → Create a free account
3. Verify your email

---

## **Step 2: Connect GitHub**

1. In Render Dashboard → Click **"New"** → Select **"Web Service"**
2. Click **"Connect account"** → Authorize GitHub
3. Select repository: **CO-PO-Lab-Attainment-Automation**
4. Click **"Connect"**

---

## **Step 3: Configure Service**

Fill in the following details:

| Field | Value |
|-------|-------|
| **Name** | `co-po-lab-attainment` |
| **Environment** | `Python 3` |
| **Build Command** | `pip install -r requirements.txt && python manage.py migrate` |
| **Start Command** | `gunicorn config.wsgi:application` |
| **Plan** | `Free` (or Paid if needed) |

---

## **Step 4: Set Environment Variables**

Click **"Advanced"** → Add these environment variables:

```
SECRET_KEY = [Render will auto-generate a secure key]
DEBUG = False
ALLOWED_HOSTS = your-app-name.onrender.com
DATABASE_URL = [Will be auto-generated when you add PostgreSQL]
```

---

## **Step 5: Add PostgreSQL Database**

1. Still in **"Advanced"** section
2. Click **"Add Database"**
3. Select **"PostgreSQL"**
4. Choose **"Free"** plan
5. Click **"Create"**

Render will automatically:
- Create a PostgreSQL database
- Add `DATABASE_URL` environment variable
- Configure backups

---

## **Step 6: Deploy!**

1. Click **"Create Web Service"**
2. Render will automatically:
   - Clone your repository
   - Install dependencies
   - Run migrations
   - Start your application

**Status**: Watch the build logs. When you see "Active", your app is live! 🎉

Your app URL will be: `https://co-po-lab-attainment.onrender.com`

---

## **Step 7: Verify Deployment**

1. Visit your app URL
2. Test login page
3. Upload a test Excel file
4. Verify reports generate correctly

---

## **Common Issues & Fixes**

### **Issue**: Build fails - `ModuleNotFoundError`
**Solution**: Make sure `requirements.txt` exists and is up-to-date
```bash
pip freeze > requirements.txt
git add requirements.txt
git commit -m "Update dependencies"
git push
```

### **Issue**: Static files not loading (CSS/JS broken)
**Solution**: Already handled with WhiteNoise middleware in updated `settings.py`

### **Issue**: Database migration fails
**Solution**: Check Render build logs. Render auto-runs:
```bash
python manage.py migrate
```

### **Issue**: "DisallowedHost" error
**Solution**: Add your Render domain to `ALLOWED_HOSTS` in environment variables

---

## **Auto-Deploy on Every Push**

After initial setup, Render automatically:
- Watches your GitHub repository
- Detects changes on the `main` branch
- Rebuilds and redeploys
- Zero downtime deployments

Just push your code:
```bash
git add .
git commit -m "Your changes"
git push origin main
```

---

## **Monitoring & Logs**

In Render Dashboard:

1. **Logs**: View real-time application logs
2. **Metrics**: CPU, Memory, Requests
3. **Events**: Build history and deployments
4. **Settings**: Update environment variables anytime

---

## **Scaling (When You Need It)**

When free tier isn't enough:

1. Go to your service settings
2. Upgrade to **"Paid"** plan ($7-100/month)
3. Choose instance type (better CPU, more RAM)
4. Render handles auto-scaling

---

## **Backup & Database Management**

PostgreSQL on Render includes:
- ✅ Automatic daily backups
- ✅ Point-in-time recovery
- ✅ High availability option
- ✅ Connection pooling

---

## **Custom Domain (Optional)**

Want `mycollege.com` instead of `*.onrender.com`?

1. Buy domain from GoDaddy, Namecheap, etc.
2. In Render → Settings → Custom Domain
3. Add DNS records (Render provides instructions)
4. Wait 24 hours for propagation

---

## **SSL Certificate**

✅ Automatically included! 
- Free SSL/TLS for all apps
- Auto-renewal
- Enforced HTTPS

---

## **Troubleshooting Deployment**

### **Check Build Logs**:
```
Render Dashboard → Events → See detailed build errors
```

### **SSH into Running App** (Advanced):
```bash
render ssh
python manage.py createsuperuser
python manage.py shell
```

### **Reset Database**:
```
In Render → PostgreSQL settings → Delete database
Render will recreate on next deploy
```

---

## **Production Checklist**

- ✅ `DEBUG = False` (environment variable)
- ✅ `SECRET_KEY` is secure (auto-generated by Render)
- ✅ `ALLOWED_HOSTS` configured
- ✅ `settings.py` updated for production
- ✅ `requirements.txt` includes all dependencies
- ✅ Database migrations run automatically
- ✅ Static files collected (WhiteNoise handles it)
- ✅ GitHub repository connected
- ✅ Environment variables set in Render

---

## **Next Steps**

1. ✅ Go live on Render
2. Add custom domain
3. Setup email notifications
4. Configure backup retention
5. Monitor application performance
6. Scale if needed

---

## **Support**

- **Render Docs**: https://render.com/docs
- **Django Docs**: https://docs.djangoproject.com
- **GitHub Issues**: Create issues in your repo

---

## **Cost Breakdown**

| Service | Free Tier | Paid |
|---------|-----------|------|
| **Web Service** | Free (sleeps after 30 min inactivity) | $7+/month |
| **PostgreSQL** | 256 MB free | Pay as you grow |
| **Bandwidth** | Included | Included |
| **SSL** | Free | Free |

**Total for Production**: ~$15-30/month (very affordable!)

---

**Deployed with ❤️ using Render.com**

Questions? Check the Render docs or GitHub issues!
